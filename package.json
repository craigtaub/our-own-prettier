{
  "name": "our-own-prettier",
  "version": "1.0.0",
  "description": "",
  "main": "./client/out/extension",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w"
  },
  "author": "",
  "license": "ISC",
  "publisher": "craig-vscode-extension",
  "activationEvents": [
    "onCommand:extension.craigifyIt"
  ],
  "contributes": {
    "commands": [
      {
        "command": "extension.craigifyIt",
        "title": "Craigify it"
      }
    ]
  },
  "configuration": {
    "type": "object",
    "title": "Craigs example configuration",
    "properties": {
      "languageServerExample.maxNumberOfProblems": {
        "scope": "resource",
        "type": "number",
        "default": 100,
        "description": "Controls the maximum number of problems produced by the server."
      }
    }
  },
  "engines": {
    "vscode": "^1.43.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.0",
    "typescript": "^3.9.4",
    "abstract-syntax-tree": "^2.8.1"
  }
}
